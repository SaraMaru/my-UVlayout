有什么因素可能导致用户放弃自动展UV而选择手动操作？
面片缺乏逻辑性——》分裂程度可以控制，自动生成切割线，随后用户可以手动加减线，程序也提供自动延展，也能够在存在用户指定的切割线的基础上按自动生成按钮
不同部位精细度不同——》可以调节面片大小
不喜欢方向与布局——》可以调节面片方向与移动面片

拓展功能：
给定3D模型，自动生成纸模展开图

论文细节：
论文里的UV太细碎了，恐怕不适合手工画贴图，不过用工具来画应该没问题
目前貌似只支持三角面？
对低精度的模型效果很差——》自动适应的阈值
要提供生成obj功能（把UV信息写在obj中）
assimp读取我生成的obj文件会有边数与顶点数的错误，不知道是不是assimp的问题（读其它obj也有问题）
无法有效处理位于同一个mesh但与周围面片都不相邻的面片，例如ma.obj

用时：
segmentation 17s
OpenNL 5*v_num 17s
Eigen v_num/2 2min58s
Eigen v_num/5 1min37s
